let initialState = {
  allUsers: [],
};

const rootReducer = (state = initialState, action: any) => {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
};

export default rootReducer;
